"use client"
const { createContext } = require("react");

// Used to if accessibility bar is opened for form editor
const IsAccessOpenContext = createContext()
export default IsAccessOpenContext;